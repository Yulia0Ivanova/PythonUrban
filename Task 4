# Это задание делала еще через день. Все коды снова работают и печатают,
#  но теперь все списки, вплоть до задания 2 - "variable",
#    хотя день назад, наводя на любой из них, он показывал количество элементов и сами объекты в списке.
#  То есть ничего не печатается, потому что для кода все списки..пустые? -_-
print(a)
def filter_min_green_index(a, min_index: float = 0.1):
  min_list = []
  # Вариант 1 - не работает
  for item in a:
    if item > min_index:
     min_list.append(item)
  # Вариант 2 - тоже
  min_list = [item for item in a if item > min_index]
  print("Число территорий, индекс озеленения которых ниже min_index:", min_list)
