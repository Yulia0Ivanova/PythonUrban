# Случилась очень странная вещь: на паре 07.10 все сработало, но когда я на следующий день открыла Colab,
#страница написала, что "изменения не были сохранены", я ничего не меняла, сохранила, перезагрузила.
#  Отображение результатов сначала было, но потом я нажала на "Запуск кода" - Два предыдущих кода сработали, а этот - нет. 
# Я пробовала пересохранять или что-то переименовать\переписать - постоянно одна ситуация: ошибки нет, код выполняется, но ничего не печатается.
# Вроде бы суть проблемы в том, что он не понимает что такое green_index (хотя в 1 задании все правильно написано)
    #    def get_mean_green_index(list_green_zone_index):
    #     for item in list_green_zone_index:
    #       print(item.green_index)
    
   # Поэтому я смогла сделать только так))
def get_mean_green_index(a, b):
  return b
a = [item.calculate_green_index() for item in list_green_zone_index]
b = sum(a) / len(a)
print(a)
print("Средний индекс озеленения", round(b, 2)) 
